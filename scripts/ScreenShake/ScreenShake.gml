/// @description ScreenShake(xrange, yrange, anglerange, intensity, ease)
/// @param xrange
/// @param yrange
/// @param anglerange
/// @param intensity
/// @param ease

with(oCamera){
    xrange = argument0;
    yrange = argument1;
    anglerange = argument2;
    intensity = argument3;
    ease = argument4;
}